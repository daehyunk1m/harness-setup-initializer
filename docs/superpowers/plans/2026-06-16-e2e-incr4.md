# E2E 모듈 마감 (이슈 #12 증분 4 / TODO-97) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. 이 작업은 단일 working repo의 계약-임계 순차 문서 편집이므로 **inline 실행**(서브에이전트 분산 금지 — 동일 SKILL.md 동시 편집 충돌 위험).

**Goal:** 1.11.0~1.16.0에 걸쳐 만든 E2E 옵트인 모듈을 마감한다 — managed 자동 감지 매핑 정렬(A), 프리셋 권장 기본(B), 업그레이드 U1 재감지(C), 사람 개발자용 작성 가이드(D). → 버전 1.17.0.

**Architecture:** 전부 옵트인·생략 기본·신규 플레이스홀더 0(31 불변)·마이그레이션 불필요. managed 편집은 §12.6 자동 감지로 기존 하네스에 전파. 두 SKILL.md 프로필 스키마 JSON 블록 byte-identical 유지.

**Tech Stack:** Markdown 스펙(SKILL.md ×2), JSON 프리셋, 신규 Markdown 템플릿, bash 골든 픽스처.

**확정 결정:** D1=pre-seed · D2=e2e/README.md managed · D3=cascade · D4=단일 1.17.0 (설계 doc §0).

---

### Task A: §12.6.1 e2e managed 매핑 정렬 (무결정, 먼저)

**Files:**
- Modify: `SKILL.md:1146-1148`

- [ ] **Step 1: §12.6.1 매핑 테이블에 e2e config 2행 추가**

`SKILL.md:1146`의 `| .githooks/pre-push | templates/githooks/pre-push |` 행 **뒤**에 2행 추가:
```
| `playwright.config.ts` | `templates/playwright.config.ts` |
| `e2e/tsconfig.json` | `templates/e2e/tsconfig.json` |
```
(README 행은 Task D에서 추가 — 본 Task는 config 2행만.)

- [ ] **Step 2: SKILL.md:1148 deferral 노트 갱신**

기존 노트(e2e managed 부재 단언 + "증분 4로 이월")를 다음으로 교체:
```
> **노트**: e2e managed 파일(`playwright.config.ts`, `e2e/tsconfig.json`, `e2e/README.md`)도 위 매핑에 포함된다 — 옵트인(`e2e.enabled`) 하네스에서만 생성되며, 매핑이 있으므로 다음 업그레이드부터 § 12.6 자동 감지 대상이 된다(템플릿 변경 전파). custom e2e 파일(`e2e/fixtures/test.ts`·`seed.ts`·`e2e/specs/smoke.e2e.ts`)은 사용자 소유라 매핑에서 제외한다.
```

- [ ] **Step 3: 검증 — deferral 잔여·테이블 정합**

Run: `cd /Users/daehyun/.claude/skills/harness-setup && grep -n "증분 4 / 본 증분 범위 밖\|증분 4로 이월\|매핑에 \*\*부재\*\*" SKILL.md`
Expected: 출력 없음 (deferral 주석 제거됨)
Run: `grep -n "playwright.config.ts\|e2e/tsconfig.json" SKILL.md | head`
Expected: §12.6.1 테이블에 2행 존재

- [ ] **Step 4: Commit**

```bash
git add SKILL.md docs/superpowers/
git commit -m "$(cat <<'EOF'
docs(skill): §12.6.1에 e2e config managed 매핑 추가 (이슈 #12 증분 4 A)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task B: 프리셋 e2e 기본값 (D1=pre-seed)

**Files:**
- Modify: `presets/react-next.json`, `presets/react-router-fsd.json`, `presets/react-vite.json`
- Modify: `SKILL.md` §6 스키마(~774), §9 가이드(~858), 필드 규칙(666), §4.2(356)
- Modify: `harness-scaffold/SKILL.md:183`

- [ ] **Step 1: 3개 프론트엔드 프리셋에 e2e 블록 추가**

각 파일에서 `"docFreshnessDays": 14` 뒤에 쉼표+새 줄 추가:
```json
  "docFreshnessDays": 14,

  "e2e": { "enabled": true }
```
(react-next.json·react-router-fsd.json·react-vite.json 3개. **express-api.json 비변경**.)

- [ ] **Step 2: 모든 프리셋 valid JSON 확인**

Run: `for f in presets/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" && echo "OK $f"; done`
Expected: 4개 모두 OK, express-api 포함

- [ ] **Step 3: §6 프리셋 스키마에 선택 e2e 필드 문서화**

`SKILL.md`의 §6 스키마 JSON(`docFreshnessDays: 14` ~767-774 근처)에 선택 필드 주석 추가 + 스키마 아래 산문 1줄: "`e2e`(선택)는 프론트엔드 프리셋이 E2E 계층의 **권장 기본**(`{"enabled": true}`)을 제공한다. `playwrightVersion`은 프리셋에 하드핀하지 않는다(스킬보다 빨리 stale) — §4.2가 현재 stable로 채운다. `prePush`/`mcp` 기본값은 시드하지 않는다(독립 2차 옵트인)."

- [ ] **Step 4: §9 커스텀 프리셋 가이드에 선택 필드 1줄**

`SKILL.md:859`(detection.exclude 항목) 근처에 선택 필드 안내 1줄 추가: "(선택) `e2e: {"enabled": true}` — 프론트엔드 프리셋에 E2E 권장 기본을 둘 수 있다(§4.2 옵트인 질문은 유지)."

- [ ] **Step 5: 필드 규칙 prose 재서술 (3곳 일관)**

`SKILL.md:666` 필드 규칙 행: "프리셋 비대상 (감지+문답 전용)" → "프론트엔드 프리셋은 **권장 기본**(`enabled:true`)을 제공하나 §4.2 옵트인 확인은 유지(거부/무응답=생략)".
`SKILL.md:354-356` §4.2: 거부/무응답=생략 계약 유지하되 "프리셋이 권장한 경우 기본 답을 '예'로 제시" 1구 추가.
`harness-scaffold/SKILL.md:183` 미러: "프론트엔드 프로젝트에서만 제안된다." → "프론트엔드 프리셋이 권장 기본을 제공할 수 있으나, 최종적으로 프로필에 `e2e`가 있을 때만 생성한다(옵트인 계약 불변)." content 일관.

- [ ] **Step 6: 머지 우선순위 확인 (무변경 가능)**

`SKILL.md:433` "프리셋 값을 기본으로 하되, 문답에서 다르게 확인된 부분을 오버라이드한다" — 이 문구가 프리셋 e2e 권장기본 → §4.2 거부 시 오버라이드(생략)를 이미 지배. **무변경**(확인만).

- [ ] **Step 7: 검증 — byte-identical JSON 블록·31 불변**

Run: `python3 - <<'PY'
import re
def block(p):
    t=open(p).read(); i=t.index('```json',t.index('프로필')); 
    s=t.index('{',i); 
    # naive: capture to first ``` after
    j=t.index('```',s); return t[s:j]
a=block('SKILL.md'); b=block('harness-scaffold/SKILL.md')
print('IDENTICAL' if a==b else 'DIFFER')
PY`
Expected: IDENTICAL (version 둘 다 1.16.0인 현 시점; 버전 범프는 Task 버전에서 동시 처리)
Run: `grep -c "{{" SKILL.md harness-scaffold/SKILL.md` — 변동 없음(프리셋 필드는 플레이스홀더 아님)

- [ ] **Step 8: Commit**

```bash
git add presets/ SKILL.md harness-scaffold/SKILL.md
git commit -m "$(cat <<'EOF'
feat(presets,skill): 프론트엔드 프리셋 e2e 권장 기본 + 옵트인 유지 (이슈 #12 증분 4 B)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task C: U1 재감지 (D3=cascade)

**Files:**
- Modify: `harness-scaffold/SKILL.md:1737-1743`

- [ ] **Step 1: E2E 재감지 불릿 prepend (1743 앞)**

`harness-scaffold/SKILL.md:1743`(`- **pre-push 재감지**…`) **앞**에 새 불릿:
```
- **E2E 재감지** (1.17.0+ U1): 프론트엔드로 감지됐는데(SKILL.md:180 동일 신호 재사용) 프로필에 `e2e` 블록(특히 `e2e.enabled`)이 없으면, 업그레이드 U1 재감지에서 브라우저 E2E 계층(Playwright) 옵트인을 **제안**한다(생략 기본 — 거절/무응답 시 산출물 0건). 수락 시 § 5.17 생성(playwright.config.ts·e2e/tsconfig.json·e2e/README.md managed + fixtures/seed/smoke custom) + manifest 등록(profile.e2e + 신규 templateHash). `@playwright/test`는 § 5.5 add-only 머지 후 Phase U5 수동 안내(스킬 미설치). **cascade**: 수락한 경우 이어서 pre-push 재감지(아래)를 같은 U1 패스에서 평가한다(신규셋업 SKILL.md:357 미러) — 즉 아래 pre-push 재감지 전제의 "기존 `e2e.enabled`"는 "기존 또는 금번 수락한 `e2e.enabled`"로 읽는다.
```

- [ ] **Step 2: 무마이그레이션 프로즈에 1.17.0 노트 추가 (1741 뒤)**

`harness-scaffold/SKILL.md:1741`(1.16.0 노트) **뒤**, 1742 공백 줄 앞에 추가:
```
>
> **1.17.0**(이슈 #12 증분 4, E2E 모듈 마감)도 마이그레이션이 없다 — 프리셋 e2e 권장 기본(B)·§12.6.1 매핑 정렬(A)·U1 재감지(C)·가이드 문서(D) 모두 옵트인·생략 기본이라 기존 프로필에 추가 불필요하고, 기존 e2e 미옵트인 하네스의 산출물은 0건이다. `e2e/README.md`는 신규 managed 파일이지만 `e2e.enabled` 옵트인 시에만 생성된다(1.11.0/1.14.0 선례). 기존 **e2e 옵트인** 하네스는 §12.6.1에 추가된 매핑(playwright.config.ts·e2e/tsconfig.json·e2e/README.md)으로 다음 업그레이드부터 자동 감지·재렌더 대상이 된다(템플릿 변경 전파 — 신규 파일 생성 아님). 추가 신규 파일은 U1 재감지 옵트인 경로로만 발생한다 (Phase U1 재감지).
```

- [ ] **Step 3: 검증 — 읽기 순서·cascade 문구**

Run: `grep -n "E2E 재감지\|pre-push 재감지\|MCP 재감지" harness-scaffold/SKILL.md`
Expected: 순서 E2E 재감지 → pre-push 재감지 → MCP 재감지
Run: `grep -c "1.17.0" harness-scaffold/SKILL.md`
Expected: ≥1 (무마이그레이션 노트)

- [ ] **Step 4: Commit**

```bash
git add harness-scaffold/SKILL.md docs/superpowers/
git commit -m "$(cat <<'EOF'
feat(skill): U1 base-E2E 재감지 + pre-push cascade (이슈 #12 증분 4 C)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task D: 사용자 E2E 작성 가이드 (D2=e2e/README.md managed)

**Files:**
- Create: `templates/e2e/README.md`
- Modify: `harness-scaffold/SKILL.md` §5.17(1027-1040), §10.1(1445-1450), step19(223), Phase4(1321)
- Modify: `SKILL.md` §12.6.1(1146 근처 — README 행)
- Modify: `references/harness-checklist.md`(118 근처), `references/versioning-policy.md:224`

- [ ] **Step 1: templates/e2e/README.md 생성 (사람-온보딩, 정적 리터럴)**

내용(8섹션, 에이전트-규칙 내용은 참조만, 명령은 정적 `npm run test:e2e`): 설계 doc §2-D + 핸드오프 §D 개요. 핵심:
1. 무엇/언제 — `npm run test:e2e`(패키지매니저에 맞춰 npm/yarn/pnpm), validate와 분리, VERIFY(E2E) Phase 4.7 매핑. L1-L4는 coding-standards 참조 1줄.
2. e2e/ 레이아웃 — specs/`{featureID}-{slug}.e2e.ts`(testMatch `**/*.e2e.ts`), fixtures/test.ts(fresh context), fixtures/seed.ts.
3. fixtures·seed — `page.addInitScript` 시드 주입, persist-envelope. 앱 부팅(env·auth route-block·clock)은 playwright.config.ts 주석 참조(복제 금지).
4. 셀렉터·태그 — data-testid 우선, `@feature:{ID}`(VERIFY 선택 실행), `@critical`(pre-push 전용 — coding-standards 참조).
5. 시각/레이아웃 회귀 트리거(1.16.0) — geometry 의존이면 상호작용 없어도 E2E, jsdom 한계. 측정 단언(boundingBox/scrollHeight). 육안 1회는 가드 아님→코드화. (test-engineer.md·coding-standards.md 참조.)
6. 실행 — test:e2e, `--grep @feature/@critical`, 최초 `npm i && npx playwright install`(스캐폴드 미설치).
7. (조건부, e2e.mcp 시) debugger §0.5 1줄 포인터 — "if wired" 정적 문구.
8. (조건부, e2e.prePush 시) @critical cross-feature 강제 1줄 — "if wired" 정적 문구.

- [ ] **Step 2: 미치환 플레이스홀더 0 확인**

Run: `grep -c "{{" templates/e2e/README.md`
Expected: 0 (정적 리터럴 — §6.11 grep 추가 불필요)

- [ ] **Step 3: §5.17 생성 테이블 + 규칙에 README 행**

`harness-scaffold/SKILL.md:1030`(e2e/tsconfig.json 행) 뒤에:
```
| `e2e/README.md` | managed | `templates/e2e/README.md` | 없음 |
```
§5.17 규칙(1039 근처)에 1줄: "`e2e/README.md`는 사람 개발자용 정적 가이드(managed, 플레이스홀더 0) — §12.6 자동 감지로 갱신 전파. 에이전트-규칙 내용은 복제하지 않고 참조한다."

- [ ] **Step 4: §10.1 분류 테이블 + step19 + Phase4 카탈로그**

§10.1(`harness-scaffold/SKILL.md:1450` `.githooks/pre-push` #31 앞)에 README 행 삽입(#31), pre-push #31→#32:
```
| 31 | `e2e/README.md` | managed | E2E 작성 가이드 (사람 개발자용, e2e 옵트인 시에만) |
| 32 | `.githooks/pre-push` | managed | … (기존 문구) |
```
step19(223) 설명: "playwright.config.ts + e2e/ 디렉토리" → "playwright.config.ts + e2e/ 디렉토리(+ e2e/README.md 가이드)".
Phase4 카탈로그(1321) 기존 E2E 줄 끝 상세 포인터: "(상세: e2e/, playwright.config.ts)" → "(상세: e2e/README.md · playwright.config.ts)".

- [ ] **Step 5: §12.6.1(SKILL.md)에 README 매핑 행 (발견 1)**

`SKILL.md`의 §12.6.1 테이블(Task A에서 추가한 e2e/tsconfig.json 행 뒤)에:
```
| `e2e/README.md` | `templates/e2e/README.md` |
```

- [ ] **Step 6: checklist §4.2 + versioning-policy 1.17.0 행**

`references/harness-checklist.md` §4.2(123 뒤)에 1줄: "(1.17.0, 증분 4) e2e 옵트인 시 `e2e/README.md`(사람 개발자 작성 가이드, managed)가 생성된다 — 무엇/언제·레이아웃·fixtures·셀렉터·시각회귀 트리거·실행을 정본 참조와 함께 안내."
`references/versioning-policy.md:224` 뒤에 행:
```
| 1.17.0 릴리스 | E2E 모듈 마감(이슈 #12 증분 4): §12.6.1 매핑 정렬·프리셋 권장 기본·U1 재감지·작성 가이드(e2e/README.md managed). 신규 플레이스홀더 0(31 불변)·옵트인·마이그레이션 불필요. | MINOR | 새 옵트인 산출물 + 기존 e2e managed 자동 감지 편입 — 기존 하네스 호환 |
```

- [ ] **Step 7: 검증 — 번호 하드참조 없음·정합**

Run: `grep -rn "31번\|#31\|31\. " harness-scaffold/SKILL.md SKILL.md | grep -i "pre-push\|githook"`
Expected: 출력 없음 (pre-push가 번호로 하드참조되지 않음 — 이름 참조만)
Run: `grep -n "e2e/README.md" harness-scaffold/SKILL.md SKILL.md references/`
Expected: §5.17 테이블·§10.1·step19·Phase4·§12.6.1·checklist에 출현

- [ ] **Step 8: Commit**

```bash
git add templates/e2e/README.md harness-scaffold/SKILL.md SKILL.md references/ docs/superpowers/
git commit -m "$(cat <<'EOF'
feat(templates,skill): 사용자 E2E 작성 가이드(e2e/README.md managed) (이슈 #12 증분 4 D)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task V: 버전 범프 1.16.0 → 1.17.0 + 트래킹

**Files:**
- Modify: `SKILL.md:549`, `harness-scaffold/SKILL.md:77` (프로필 스키마 version)
- Modify: `references/project-context.md` 버전 히스토리
- Modify: `.tracking/CHANGELOG.md`, `.tracking/HANDOFF.md`, `.tracking/TODO.md`

- [ ] **Step 1: 두 SKILL.md 프로필 스키마 version 동시 범프**

`SKILL.md:549`·`harness-scaffold/SKILL.md:77` 둘 다 `"version": "1.16.0"` → `"1.17.0"`.

- [ ] **Step 2: byte-identical JSON 블록 재확인**

Run: (Task B Step 7의 python diff 재실행)
Expected: IDENTICAL (둘 다 1.17.0)

- [ ] **Step 3: project-context.md 버전 히스토리 + CHANGELOG + HANDOFF + TODO**

- `references/project-context.md`: 버전 히스토리에 1.17.0 엔트리(증분 4 마감, 4항목, D1-D4 결정), § 설계 결정에 발견 1(README §12.6.1 매핑) 기록.
- `.tracking/CHANGELOG.md`: 1.17.0 Added/Changed 분류.
- `.tracking/HANDOFF.md`: Session 42 요약, 현재 버전 1.17.0, P7 커버리지 1.17.0 줄, 우선순위 목록에서 TODO-97 제거(완료 표시).
- `.tracking/TODO.md`: TODO-97 완료 체크 + 결과 기록.

- [ ] **Step 4: 전체 검증 게이트 (설계 doc §3)**

Run: `cd /Users/daehyun/.claude/skills/harness-setup && bash test/run-fixtures.sh && bash test/e2e-fixtures.sh && bash test/mcp-fixtures.sh && bash test/prepush-fixtures.sh 2>&1 | tail -30`
Expected: 모든 픽스처 통과
Run: `grep -rn "31개" SKILL.md references/versioning-policy.md` — "31" 불변 확인
Run: `git grep -n "증분 4로 이월\|범위 밖" SKILL.md` — 잔여 deferral 없음

- [ ] **Step 5: Commit + tag**

```bash
git add -A
git commit -m "$(cat <<'EOF'
docs(tracking,skill): 1.17.0 — E2E 모듈 마감 버전 범프 + 트래킹 (이슈 #12 증분 4, TODO-97)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
git tag -a v1.17.0 -m "1.17.0 — E2E 모듈 마감 (이슈 #12 증분 4)"
```

---

### Task R: 적대적 검증 (릴리스 전 최종)

- [ ] **Step 1: Workflow 적대적 리뷰** — 독립 에이전트가 각 계약을 검증:
  - 두 SKILL.md e2e 필드 prose content 일관 + JSON 블록 byte-identical
  - 31 플레이스홀더 불변, §6.11 grep 커버리지(정적 리터럴이라 README 미포함이 정상)
  - §12.6.1에 e2e managed 3파일 모두 매핑(자동 감지 결정론)
  - Phase4 카탈로그 순수 투영(e2e.enabled 게이트, 미와이어 능력 광고 없음)
  - 무마이그레이션 주장 타당성(옵트인·생략 기본)
  - express-api.json 호환(e2e 없음)
  - README가 에이전트-규칙 내용을 복제하지 않고 참조

- [ ] **Step 2: 발견 수정 후 재검증** (있으면)

---

## Self-Review (작성자 체크)

- **Spec 커버리지**: 핸드오프 A·B·C·D + D1-D4 결정 + 발견 1·2 → Task A/B/C/D/V/R로 전부 매핑. ✅
- **Placeholder 스캔**: 모든 Step에 실제 문자열·명령 포함. ✅
- **타입 일관성**: `e2e/README.md` 경로·managed 카테고리·§12.6.1 매핑이 모든 Task에서 동일. pre-push #31→#32 재번호 일관. ✅
