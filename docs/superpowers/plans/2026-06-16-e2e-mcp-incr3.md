# Playwright MCP 연계 (이슈 #12 증분 3) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 프론트엔드 하네스에 옵트인 Playwright MCP 탐색 진단을 배선한다 — 공유 `.mcp.json` 없이 debugger.md 지침 + 개발자 로컬 `claude mcp add` 등록으로.

**Architecture:** 신규 파일 0개. `e2e.mcp` 프로필 서브필드(분리 옵트인)가 debugger.md(managed)의 단일 플레이스홀더 `{{MCP_DEBUG_PROTOCOL}}`를 조건부 블록으로 치환한다(`{{INTEGRATION_NOTES}}` 패턴). 버전은 `e2e.mcp.version`을 scaffold가 인라인. 검증은 골든 픽스처(bash).

**Tech Stack:** Markdown 템플릿(debugger.md), 두 SKILL.md(분석/스캐폴딩 사양), bash 골든 픽스처(`test/*.sh`), 하네스 스킬 시스템(프로필→scaffold 치환).

**설계 정본:** `docs/superpowers/specs/2026-06-16-e2e-mcp-incr3-design.md` (D1~D6, §9 정합성 계약).

**목표 버전:** 1.15.0 (MINOR — 플레이스홀더 30→31, `e2e.mcp` 서브필드). 마이그레이션 불필요(옵트인·생략 기본).

---

## File Structure

| 파일 | 책임 | 변경 |
|------|------|------|
| `templates/agents/debugger.md` | TDD debugger 에이전트 정의 | 수정 — §0 뒤에 `{{MCP_DEBUG_PROTOCOL}}` 삽입 |
| `harness-scaffold/SKILL.md` | 스캐폴딩 정규 사양 | 수정 — 입력 스키마 `e2e.mcp`, §5.19 MCP 블록 + 치환 규칙, Phase 4 카탈로그/보고, U1 재감지, §10.3 마이그레이션 주석 |
| `SKILL.md` | 분석 정규 사양 | 수정 — §1.4 프론트 신호 노트, §4.2 MCP 질문, 프로필 출력 스키마 `e2e.mcp`, 스키마 필드 테이블 |
| `references/harness-checklist.md` | 판정 기준 SSoT | 수정 — §4.2 MCP 탐색 진단 경로 1줄 |
| `references/versioning-policy.md` | semver 정책 | 수정 — 1.15.0 엔트리 |
| `test/mcp-fixtures.sh` | MCP 배선 골든 픽스처 | **신규** |
| `.tracking/{HANDOFF,CHANGELOG,TODO}.md`, `references/project-context.md` | 트래킹 | 수정 — Task 8 |

**계약 (설계 §9, 구현 중 동시 유지)**: SKILL.md 출력 스키마 = harness-scaffold 입력 스키마(`e2e.mcp`). `{{MCP_DEBUG_PROTOCOL}}`는 debugger.md에 정확히 1개 + scaffold 치환표에 1개 규칙. Phase 4 카탈로그 MCP 줄은 게이트 신호 `e2e.mcp.enabled`만 가짐(순수 투영).

---

## Task 1: 핀 버전 + CLI/등록 구문 확정 (착수 전 확인, 코드 변경 없음)

**Files:** 없음 (검증·기록 전용 — 결과는 Task 4·5에서 사용)

설계 §10. `@playwright/mcp`는 0.0.x pre-1.0이라 버전·플래그 드리프트 위험이 크다(codex). 구현 시점 값을 확정한다.

- [ ] **Step 1: 현재 stable 버전 확인**

Run: `npm view @playwright/mcp version`
Expected: 단일 버전 출력(예: `0.0.76`). 이 값을 **PIN**으로 채택(이 플랜 작성 시점 `0.0.76`).

- [ ] **Step 2: MCP CLI 플래그 검증**

Run: `npx -y @playwright/mcp@<PIN> --help 2>&1 | head -50`
확인: `--headless`, `--isolated`(또는 동등 격리 옵션), 도메인 제한 플래그(`--allowed-origins`/`--host` 등) 존재 여부.
- `--isolated`가 없으면 동등한 임시-프로필 플래그명으로 대체(없으면 블록에서 해당 플래그 제거 + 지침으로만 격리 강제).
- 도메인 제한 플래그가 있으면 등록 명령에 추가, 없으면 §0.5 보안 문구로만 강제(코드 강제 불가 명시 — 설계 §4.3·§10.2).

- [ ] **Step 3: `claude mcp add` 구문 확정**

Run: `claude mcp add --help 2>&1 | head -40`
확인: `claude mcp add <name> <commandOrUrl> [args...]`, 기본 `--scope local`(커밋 안 됨), `-- ` 구분자로 subprocess 인자 전달.
확정 명령(플랜 작성 시점 검증됨): `claude mcp add playwright-harness -- npx -y @playwright/mcp@<PIN> --headless --isolated`

- [ ] **Step 4: 결과 기록**

세 값(PIN 버전 / 검증된 플래그 / 등록 명령)을 Task 4·5에 반영할 메모로 남긴다. 커밋 없음.

---

## Task 2: 골든 픽스처 작성 (RED)

**Files:**
- Create: `test/mcp-fixtures.sh`

설계 §12. 신규 파일이 없으므로 **치환·정합성 회귀**를 검증한다. 이 시점엔 대상 변경이 없어 전부 FAIL해야 한다(RED).

- [ ] **Step 1: 픽스처 작성**

```bash
#!/bin/bash
# MCP 진단 배선 골든 픽스처 (이슈 #12 증분 3) — 스킬 자체 검증, 생성 프로젝트와 무관(footprint 0)
#
# 검증:
#   1. debugger.md에 {{MCP_DEBUG_PROTOCOL}} 정확히 1개
#   2. 빈 치환(미옵트인) → 잔여 {{}} 0 + §0/§1 구조 유지
#   3. scaffold 치환 규칙에 {{MCP_DEBUG_PROTOCOL}} + 블록 핵심 마커 존재
#   4. 두 SKILL.md에 e2e.mcp 스키마 동기
# 요구: grep. 사용법: bash test/mcp-fixtures.sh
set -u
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
DBG="$ROOT/templates/agents/debugger.md"
SCAFFOLD="$ROOT/harness-scaffold/SKILL.md"
SETUP="$ROOT/SKILL.md"
TMP="$(mktemp -d)"; trap 'rm -rf "$TMP"' EXIT
FAILS=0
echo "═══ MCP 진단 배선 픽스처 ═══"

# 1. debugger.md에 {{MCP_DEBUG_PROTOCOL}} 정확히 1개
N=$(grep -c '{{MCP_DEBUG_PROTOCOL}}' "$DBG" || true)
if [ "$N" = "1" ]; then echo "✅ debugger.md MCP 플레이스홀더 1개"
else echo "❌ MCP 플레이스홀더 ${N}개(기대 1)"; FAILS=$((FAILS+1)); fi

# 2. 빈 치환(미옵트인) → 잔여 {{}} 0 + §0/§1 구조 유지
sed -e 's#{{MCP_DEBUG_PROTOCOL}}##g' \
    -e 's#{{VALIDATE_COMMAND}}#npm run validate#g' \
    -e 's#{{E2E_COMMAND}}#npm run test:e2e#g' "$DBG" > "$TMP/empty.md"
if grep -q '{{.*}}' "$TMP/empty.md"; then
  echo "❌ 빈 치환 후 잔여 플레이스홀더:"; grep -n '{{.*}}' "$TMP/empty.md"; FAILS=$((FAILS+1))
else echo "✅ 빈 치환 후 잔여 0"; fi
if grep -q '진단 프로토콜' "$TMP/empty.md" && grep -q 'E2E 실패 진단' "$TMP/empty.md"; then
  echo "✅ §0/§1 구조 유지"
else echo "❌ §0/§1 구조 손상"; FAILS=$((FAILS+1)); fi

# 3. scaffold 치환 규칙 + 블록 핵심 마커 (실제 authored 블록 검증)
MISS=0
for m in '{{MCP_DEBUG_PROTOCOL}}' 'claude mcp add playwright-harness' '@playwright/mcp' '--headless' '--isolated' 'localhost' '관찰 로그'; do
  grep -qF "$m" "$SCAFFOLD" || { echo "  · scaffold 마커 누락: $m"; MISS=$((MISS+1)); }
done
if [ "$MISS" = "0" ]; then echo "✅ scaffold 치환 규칙 + 블록 마커 존재"
else echo "❌ scaffold 마커 ${MISS}건 누락"; FAILS=$((FAILS+1)); fi

# 4. 두 SKILL.md에 e2e.mcp 스키마 동기
grep -q '"mcp"' "$SETUP"    && echo "✅ SKILL.md e2e.mcp 스키마"  || { echo "❌ SKILL.md e2e.mcp 누락"; FAILS=$((FAILS+1)); }
grep -q '"mcp"' "$SCAFFOLD" && echo "✅ scaffold e2e.mcp 스키마" || { echo "❌ scaffold e2e.mcp 누락"; FAILS=$((FAILS+1)); }

echo ""; echo "═══ 판정 ═══"
if [ "$FAILS" -eq 0 ]; then echo "✅ 전체 통과 — MCP 배선 정합"; exit 0
else echo "❌ ${FAILS}건 실패"; exit 1; fi
```

- [ ] **Step 2: 실행하여 RED 확인**

Run: `bash test/mcp-fixtures.sh`
Expected: FAIL — 체크 1(플레이스홀더 없음), 3(scaffold 마커 없음), 4(스키마 없음) 실패. 체크 2는 현재도 통과 가능(잔여 0)하나 무관.

- [ ] **Step 3: 커밋**

```bash
git add test/mcp-fixtures.sh
git commit -m "test(repo): MCP 진단 배선 골든 픽스처 추가 (이슈 #12 증분 3, RED)"
```

---

## Task 3: debugger.md 플레이스홀더 삽입 (GREEN — 체크 1·2)

**Files:**
- Modify: `templates/agents/debugger.md` (§0 종료 후, `### 1. 진단 프로토콜` 앞)

- [ ] **Step 1: `{{MCP_DEBUG_PROTOCOL}}` 삽입**

`### 0. E2E 실패 진단 (브라우저 재현, 해당 시)` 절(현재 19~25줄)의 끝과 `### 1. 진단 프로토콜`(현재 27줄) 사이에 플레이스홀더 한 줄을 넣는다. 변경 전:

```markdown
   - 로직 실패면 아래 일반 진단 프로토콜을 적용한다.

### 1. 진단 프로토콜
```

변경 후:

```markdown
   - 로직 실패면 아래 일반 진단 프로토콜을 적용한다.

{{MCP_DEBUG_PROTOCOL}}

### 1. 진단 프로토콜
```

> 빈 치환 시 빈 줄만 남아 §0→§1 흐름이 자연스럽다(`{{INTEGRATION_NOTES}}` 패턴).

- [ ] **Step 2: 실행하여 체크 1·2 통과 확인**

Run: `bash test/mcp-fixtures.sh`
Expected: 체크 1 ✅(플레이스홀더 1개), 체크 2 ✅(빈 치환 잔여 0 + 구조 유지). 체크 3·4는 여전히 FAIL.

- [ ] **Step 3: 커밋**

```bash
git add templates/agents/debugger.md
git commit -m "feat(templates): debugger.md MCP 탐색 진단 플레이스홀더 (이슈 #12 증분 3)"
```

---

## Task 4: harness-scaffold/SKILL.md — 스키마·블록·렌더링 (GREEN — 체크 3)

**Files:**
- Modify: `harness-scaffold/SKILL.md` (입력 스키마 ~147·182, 치환표 §5.10.3 ~679, 신규 §5.19, Phase 4 카탈로그 ~1300, U1 ~1696, §10.3 ~1143)

- [ ] **Step 1: 입력 스키마에 `e2e.mcp` 추가 (~147줄 e2e 블록)**

`"playwrightVersion": "1.48.0"` 행이 있는 e2e 입력 스키마 JSON 예시 블록에 `mcp`를 추가한다(Task 1 PIN 버전 사용):

```jsonc
"e2e": {
  "enabled": true,
  "framework": "playwright",
  "playwrightVersion": "1.48.0",
  "prePush": true,
  "mcp": { "enabled": true, "version": "0.0.76" }   // 선택 — enabled와 독립. 0.0.76 = Task 1 PIN
}
```

- [ ] **Step 2: 입력 스키마 노트(~182줄)에 `mcp` 서명 추가**

`prePush`를 설명하는 노트 문장 뒤에 1문장 추가:

```markdown
`mcp`는 선택 객체(`enabled`/`version`)로 브라우저 MCP 탐색 진단 배선 여부를 결정한다 — `e2e.enabled`와 **독립**(스펙 없이 mcp만 가능). 신규 파일 없이 debugger.md `{{MCP_DEBUG_PROTOCOL}}` 치환만 발생(§ 5.19). `version`은 `@playwright/mcp` exact 핀.
```

- [ ] **Step 3: 신규 §5.19 — MCP 진단 배선 (옵트인) 작성**

§5.18(pre-push) 뒤에 §5.19를 추가한다. 본문에 치환 블록 전문을 둔다(Task 1 PIN·플래그 반영):

````markdown
### 5.19 MCP 진단 배선 (옵트인)

프로필에 `e2e.mcp.enabled: true`일 때만 실행한다. 신규 파일을 생성하지 않는다 — debugger.md(managed)의 `{{MCP_DEBUG_PROTOCOL}}`를 아래 블록으로 치환하고, `version`을 인라인한다. `e2e.mcp`가 없으면 빈 문자열로 치환한다(`e2e.enabled`와 무관).

치환 블록(`@playwright/mcp@{버전}`은 `e2e.mcp.version` 인라인):

```markdown
### 0.5 브라우저 MCP 탐색 진단 (스펙 없는 UI 증상, 해당 시)

재현 스펙이 아직 없는 UI 증상(사용자 버그 리포트 등)은 **라이브 브라우저로 탐색·진단**한다. 이미 실패하는 `.e2e.ts`가 있으면 § 0(러너 재현)이 정본이며 이 절을 쓰지 않는다.

**로컬 등록 (개발자 1회)** — MCP 도구는 세션 시작 시 등록돼야 사용 가능하다. 공유 `.mcp.json`을 만들지 않으므로, 진단이 필요한 개발자가 자기 로컬에 1회 등록한다:

​```sh
claude mcp add playwright-harness -- npx -y @playwright/mcp@0.0.76 --headless --isolated
​```

등록 후 세션을 재시작하면 `playwright-harness` MCP 도구가 활성화된다. 기본 `local` 스코프라 커밋되지 않는다(팀 공유 `--scope project`는 권장하지 않음 — 개인 진단 도구).

**MCP vs 러너 (정본 구분)**
- **known `.e2e.ts` 실패** → § 0 적용. 러너/아티팩트(trace·report·screenshot)가 **정본**. MCP 탐색을 시작하지 않는다.
- **스펙 없는 UI 증상** → MCP 허용. 목적은 **관찰 + 최소 재현 수집**.
- 모호하면 러너/스펙 우선, MCP는 마지막 수단.

**보안 기본값** — `--headless --isolated`(영속 프로필 금지 → 쿠키/세션 노출 차단). **localhost/devServer origin만** 기본, 외부 URL은 사용자 명시 승인. exact 핀만(`@latest` 금지).

**사용 후 의무** — MCP로 찾은 재현을 `.e2e.ts`로 코드화(E2E 모듈 옵트인 시) 또는 명시적 재현 단계로 기록한다. **최종 완료 판단은 항상 § 3의 검증 명령(validate/E2E) 통과** — MCP 관찰만으로 "수정 완료" 단정 금지.

**관찰 로그 필수** — URL · 시작 상태 · 클릭/입력 순서 · actual vs expected · console/network 근거. 본 것만 기록, 추정은 추정 표시(§ 0 플레이키니스 환각 금지 연장).
```

> Task 1에서 `--isolated`/도메인 플래그가 다르면 등록 명령을 그에 맞게 교정한다. 도메인 제한 플래그 부재 시 "localhost only"는 코드 강제 불가 — 위 보안 문구로만 강제한다.
````

- [ ] **Step 4: 치환표(§5.10.3)에 `{{MCP_DEBUG_PROTOCOL}}` 행 추가**

먼저 debugger.md 플레이스홀더가 문서화된 표를 찾는다:

Run: `grep -n '{{VALIDATE_COMMAND}}' harness-scaffold/SKILL.md`

debugger 에이전트 플레이스홀더 치환표(§5.10.3, ~679줄)에 행을 추가한다:

```markdown
| `{{MCP_DEBUG_PROTOCOL}}` | `e2e.mcp.enabled`면 § 5.19 블록으로 치환(`version` 인라인), 아니면 빈 문자열. debugger.md 전용 | (§ 5.19 블록 또는 빈칸) |
```

- [ ] **Step 5: Phase 4 카탈로그 MCP 줄 + 렌더링 규칙 (~1300줄)**

"이제 할 수 있는 일" 카탈로그에 줄을 추가하고, "브라우저 E2E 줄" 규칙(현 1309줄) 바로 뒤에 MCP 규칙을 단다:

카탈로그 줄(블록 안):
```markdown
- 브라우저 MCP 진단 → debugger가 스펙 없는 UI 증상을 라이브 브라우저로 탐색 (상세: agents/debugger.md § 0.5)
```
렌더링 규칙:
```markdown
- **브라우저 MCP 줄**: `e2e.mcp.enabled === true`일 때만 렌더 — debugger.md MCP 블록 치환을 결정한 바로 그 신호를 재사용하는 순수 투영. 미옵트인 시 줄 생략(미와이어 능력 광고 불가).
```

- [ ] **Step 6: Phase 4 보고 — 등록 명령 + 보안 고지**

Phase 4 보고 절에 `e2e.mcp.enabled` 시 출력 1블록 추가:
```markdown
- (e2e.mcp 옵트인 시) 브라우저 MCP 진단: 로컬 1회 등록 `claude mcp add playwright-harness -- npx -y @playwright/mcp@{version} --headless --isolated` (기본 local 스코프=커밋 안 됨). ⚠️ MCP 서버는 세션에서 코드를 실행한다 — 신뢰할 수 있는 환경에서만. 상세: agents/debugger.md § 0.5
```

- [ ] **Step 7: U1 재감지 (~1696줄, pre-push 재감지 뒤)**

```markdown
- **MCP 재감지** (1.15.0+ U1): 기존 프론트엔드 하네스에 `e2e.mcp`가 없으면 업그레이드 U1에서 브라우저 MCP 진단 옵트인을 **제안**한다(생략 기본 — 거절/무응답 시 산출물 0건). 수락 시 debugger.md `{{MCP_DEBUG_PROTOCOL}}`를 § 5.19 블록으로 재치환(managed 재렌더링).
```

- [ ] **Step 8: §10.3 마이그레이션 주석에 1.15.0 추가 (~1143 근처 또는 M-엔트리 주석)**

"M-1.4.0 이후 마이그레이션 불필요"류 주석에 1.15.0을 포함(옵트인·생략 기본 → 별도 M-엔트리 없음).

- [ ] **Step 9: 실행하여 체크 3 통과 확인**

Run: `bash test/mcp-fixtures.sh`
Expected: 체크 3 ✅(scaffold 마커 전부), scaffold 측 체크 4 ✅. SKILL.md 측 체크 4는 여전히 FAIL.

- [ ] **Step 10: 커밋**

```bash
git add harness-scaffold/SKILL.md
git commit -m "feat(skill): harness-scaffold MCP 배선 — e2e.mcp 스키마·§5.19 블록·카탈로그·U1 (이슈 #12 증분 3)"
```

---

## Task 5: SKILL.md — 감지·질문·출력 스키마 (GREEN — 체크 4 완성)

**Files:**
- Modify: `SKILL.md` (§1.4 ~180, §4.2 ~354, 출력 스키마 ~612, 스키마 필드 테이블 ~661)

- [ ] **Step 1: §1.4 프론트 신호 노트(~180줄)에 MCP 트리거 추가**

E2E 질문 트리거 문장 뒤에 추가:
```markdown
같은 프론트엔드 신호는 § 4.2의 브라우저 MCP 진단 질문(옵트인)도 트리거한다 — E2E 스펙 옵트인과 **독립**.
```

- [ ] **Step 2: §4.2에 MCP 질문 추가 (E2E 블록 ~354~358 뒤)**

prePush 질문(358줄) 뒤, superpowers 블록(360줄) 앞에 독립 질문을 추가한다:

```markdown
**브라우저 MCP 진단 (Playwright MCP) 관련** (Step 1.4에서 프론트엔드 신호가 감지된 경우에만, 우선순위 5 — 옵트인, E2E 스펙 옵트인과 독립):
- "에이전트가 재현 스펙이 없는 UI 증상을 **라이브 브라우저로 탐색·진단**하도록 브라우저 MCP(공식 `@playwright/mcp`)를 셋업할까요? 공유 설정 파일은 만들지 않고, debugger 에이전트 지침 + 개발자가 자기 로컬에 1회 등록하는 명령(`claude mcp add ... npx @playwright/mcp`)만 안내합니다. headless·격리·localhost 기본. E2E 스펙 작성과 무관하게 옵트인할 수 있습니다."
- 동의하면 프로필에 `e2e.mcp` 객체(`enabled: true`, `version`)를 기록한다. `version`은 묻지 않고 현재 stable(`@playwright/mcp`)로 채운다(`playwrightVersion` 기본값 패턴과 동일). 거부/무응답이면 `mcp`를 생략한다. **`e2e.enabled` 없이 `e2e.mcp`만 기록 가능** — 이 경우 `e2e` 블록은 `mcp`만 가진다.
```

- [ ] **Step 3: 프로필 출력 스키마(~612줄 e2e 블록)에 `mcp` 추가**

harness-scaffold 입력 스키마(Task 4 Step 1)와 **동일 텍스트**로:
```jsonc
"e2e": {
  "enabled": true,
  "framework": "playwright",
  "playwrightVersion": "1.48.0",
  "mcp": { "enabled": true, "version": "0.0.76" }
}
```

- [ ] **Step 4: 스키마 필드 테이블(~661줄 `e2e` 행)에 mcp 설명 추가**

`e2e` 행 끝에 1문장 추가:
```markdown
`mcp`(선택 객체 `enabled`/`version`)는 브라우저 MCP 진단 배선 — `e2e.enabled`와 독립(스펙 없이 단독 가능), 신규 파일 없이 debugger.md `{{MCP_DEBUG_PROTOCOL}}` 치환만(harness-scaffold § 5.19).
```

- [ ] **Step 5: 실행하여 전체 GREEN 확인**

Run: `bash test/mcp-fixtures.sh`
Expected: PASS — 체크 1·2·3·4 전부 ✅, "전체 통과 — MCP 배선 정합".

- [ ] **Step 6: 커밋**

```bash
git add SKILL.md
git commit -m "feat(skill): SKILL.md MCP 진단 질문·e2e.mcp 출력 스키마 (이슈 #12 증분 3)"
```

---

## Task 6: 참조 문서 정합 (harness-checklist · versioning-policy)

**Files:**
- Modify: `references/harness-checklist.md` (§4.2 또는 디버깅 절)
- Modify: `references/versioning-policy.md` (버전 히스토리/MINOR 트리거)

- [ ] **Step 1: harness-checklist.md에 MCP 탐색 진단 경로 1줄**

E2E/디버깅 관련 절에 추가:
```markdown
- (옵트인 `e2e.mcp`) 스펙 없는 UI 증상은 debugger가 브라우저 MCP(`@playwright/mcp`)로 탐색 진단 — known `.e2e.ts` 실패는 러너가 정본(MCP 비사용).
```

- [ ] **Step 2: versioning-policy.md에 1.15.0 엔트리**

MINOR 사례에 추가(플레이스홀더 +1 = 신규 기능):
```markdown
- 1.15.0: Playwright MCP 진단 배선(이슈 #12 증분 3). 플레이스홀더 30→31(`{{MCP_DEBUG_PROTOCOL}}`), 프로필 `e2e.mcp` 서브필드. 신규 파일 0·옵트인·마이그레이션 불필요.
```

- [ ] **Step 3: 커밋**

```bash
git add references/harness-checklist.md references/versioning-policy.md
git commit -m "docs(refs): 체크리스트·버전정책에 MCP 진단(1.15.0) 반영 (이슈 #12 증분 3)"
```

---

## Task 7: 전체 회귀 + 정합성 검증

**Files:** 없음 (검증). 실패 발견 시 해당 Task로 돌아가 수정.

- [ ] **Step 1: 세 픽스처 전부 실행**

Run:
```bash
bash test/run-fixtures.sh && bash test/e2e-fixtures.sh && bash test/mcp-fixtures.sh
```
Expected: 세 픽스처 모두 exit 0("전체 통과"). 기존 픽스처 회귀 없음 확인.

- [ ] **Step 2: 플레이스홀더 카운트 확인 (30→31)**

Run:
```bash
grep -rohE '\{\{[A-Z_]+\}\}' templates/ harness-scaffold/SKILL.md | sort -u | wc -l
```
Expected: 31 (또는 직전 30에서 +1). `{{MCP_DEBUG_PROTOCOL}}`가 목록에 포함되는지 확인:
```bash
grep -roh '{{MCP_DEBUG_PROTOCOL}}' templates/ | head -1
```

- [ ] **Step 3: 프로필 스키마 동기 확인 (SKILL.md ↔ harness-scaffold)**

두 파일의 `e2e` 스키마 블록에 `mcp`가 동일하게 있는지 육안/grep 대조:
```bash
grep -n '"mcp"' SKILL.md harness-scaffold/SKILL.md
```
Expected: 양쪽 모두 `"mcp": { "enabled": true, "version": ... }`.

- [ ] **Step 4: 미옵트인 무영향 확인 (회귀)**

debugger.md를 빈 치환했을 때 기존 §0·§1·Output·Constraints가 온전한지(Task 2 체크 2가 커버) 재확인. 기존 하네스(`e2e.mcp` 없음)는 빈 문자열 치환이라 무영향임을 확인.

- [ ] **Step 5: 수정 발생 시에만 커밋**

회귀/정합성 문제로 수정했으면:
```bash
git add -A && git commit -m "fix(...): 증분 3 회귀/정합성 수정"
```
문제 없으면 커밋 생략.

---

## Task 8: 버전 범프 + 트래킹

**Files:**
- Modify: `references/project-context.md`, `.tracking/HANDOFF.md`, `.tracking/CHANGELOG.md`, `.tracking/TODO.md`
- Modify: 프로필/매니페스트 스키마 version 필드(현재 `1.14.0` → `1.15.0`)

- [ ] **Step 1: version 필드 일괄 범프**

Run: `grep -rn '1\.14\.0' SKILL.md harness-scaffold/SKILL.md references/ | grep -i version`
해당 스키마 version 필드(.harness-profile / .harness-manifest 스키마)를 `1.15.0`으로 갱신. (역사 기록·마이그레이션 ID의 1.14.0은 건드리지 않음 — 현재 시제 version만.)

- [ ] **Step 2: CHANGELOG.md — 1.15.0 Added**

`## [미출시]` 또는 신규 `## [1.15.0]` 섹션 `### 추가 (Added)`에:
```markdown
- templates/agents/debugger.md: 브라우저 MCP 탐색 진단 블록(`{{MCP_DEBUG_PROTOCOL}}`) — 스펙 없는 UI 증상을 라이브 브라우저로 진단 (이슈 #12 증분 3)
- SKILL.md·harness-scaffold/SKILL.md: `e2e.mcp` 옵트인 스키마(분리 옵트인) + §5.19 배선 + Phase 4 카탈로그/보고 + U1 재감지
```

- [ ] **Step 3: HANDOFF.md — 버전·P7·TODO 갱신**

- "현재 버전" → 1.15.0.
- P7 행에 "1.15.0: MCP 탐색 진단 배선(옵트인, 증분 3) — 공유 .mcp.json 없이 debugger 지침+로컬 등록" 추가.
- §5.2 "다음 단계"에서 증분 3 완료 표기, 증분 4를 즉시 다음으로.

- [ ] **Step 4: TODO.md — TODO-96 완료**

TODO-96 상태 `[ ] 미착수` → `[x] 완료 (2026-06-16, 1.15.0)` + 해소 요약(배치 e2e 모듈·비커밋 B안·D1~D6·멀티모델 자문) + 설계/플랜 정본 경로.

- [ ] **Step 5: project-context.md — 설계 결정 + 버전 히스토리**

§ 설계 결정에 "증분 3: MCP는 e2e 모듈 확장(비통합), 공유 .mcp.json 비커밋(nagware·머지 회피) → debugger 지침+로컬 등록" 추가. 버전 히스토리에 1.15.0.

- [ ] **Step 6: 커밋**

```bash
git add references/project-context.md .tracking/ SKILL.md harness-scaffold/SKILL.md
git commit -m "docs(tracking): 1.15.0 — MCP 진단 배선 버전 범프 + TODO-96 완료 (이슈 #12 증분 3)"
```

---

## 완료 기준 (정직한 done — 설계 §12)

- **보장(검증됨)**: `e2e.mcp` 옵트인 시 debugger.md에 등록 명령(핀)·MCP-vs-러너 분기·보안·의무·관찰 로그 블록이 렌더, 잔여 플레이스홀더 0. 미옵트인 시 빈 문자열(기존 하네스 무영향). 세 골든 픽스처 통과.
- **비보장(런타임/환경 의존)**: 에이전트가 실제로 MCP를 등록·구동해 UI 버그를 진단 — 개발자 로컬 `claude mcp add` + Claude Code MCP 지원 + 앱 부팅 의존. 파일럿(haja) 도그푸딩으로만 확인(별도 후속).
- **착수 전 확인(Task 1)**: `@playwright/mcp` PIN 버전 + `--headless`/`--isolated`/도메인 플래그 + `claude mcp add` 구문이 구현 시점 값과 일치.

---

## Self-Review (작성자 체크)

- **Spec coverage**: D1(e2e 모듈=신규 파일 0, Task 3·4) / D2(비커밋=§5.19 블록·등록 명령, Task 4) / D3(분리 옵트인=§4.2 독립 질문·`e2e.mcp`, Task 5) / D4(공식·exact 핀=Task 1·블록) / D5(플레이스홀더 +1=Task 3·7 카운트) / D6(MCP-vs-러너=블록, Task 4). §5 감지/옵트인(Task 5), §6 렌더링/카탈로그(Task 4), §7 manifest/버전(Task 8), §8 U1(Task 4), §9 정합성(Task 7), §12 테스트(Task 2) — 전부 태스크 매핑됨.
- **Placeholder scan**: `<PIN>`은 Task 1이 확정하는 실값(0.0.76 기준), 플랜 내 모든 코드 블록은 실제 내용. "TBD/TODO/적절히" 없음.
- **Type consistency**: 프로필 키 `e2e.mcp.{enabled,version}`가 Task 4·5에서 동일. 플레이스홀더명 `{{MCP_DEBUG_PROTOCOL}}`가 Task 2·3·4·7에서 일관. 등록 명령 `claude mcp add playwright-harness -- npx -y @playwright/mcp@<PIN> --headless --isolated`가 Task 1·4·5에서 일관.

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
