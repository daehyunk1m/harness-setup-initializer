# E2E pre-push 인프라 (이슈 #12 증분 2b) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 프론트엔드 하네스에 옵트인 pre-push 게이트(`validate` → `@critical` E2E)를 무의존 git hook으로 추가한다.

**Architecture:** 신규 managed 템플릿 `templates/githooks/pre-push`(마커 블록·POSIX sh)를 `e2e.prePush` 옵트인 시 `.githooks/pre-push`로 생성. 활성화(`git config core.hooksPath`)는 수동(Phase 4 안내) — "승인 없이 git 실행 금지" 정합. 게이팅 로직은 골든 픽스처(mock playwright)로 결정적 검증, harness-check ⑨가 활성/비활성을 경고 전용으로 보고.

**Tech Stack:** POSIX sh (git hook), bash (픽스처 러너), Markdown (SKILL.md·harness-scaffold/SKILL.md·references). 신규 플레이스홀더 0개(기존 `{{VALIDATE_COMMAND}}` 재사용, 30 불변).

**설계 정본:** `docs/superpowers/specs/2026-06-16-e2e-prepush-2b-design.md` (결정 D1~D5, 난제 매핑 §12).

---

## File Structure

**신규 (2):**
- `templates/githooks/pre-push` — 훅 템플릿. 책임: validate→@critical 게이팅 1개 (마커 블록).
- `test/prepush-fixtures.sh` — 골든 픽스처 러너. 책임: 훅 게이팅 분기 + harness-check ⑨ 회귀 검증.

**수정 (7):**
- `templates/harness-check.sh` — ⑨ pre-push 게이트 검사(경고 전용) 추가.
- `SKILL.md` — Phase 1 `e2e.prePush` 옵트인 질문, 프로필 출력 스키마, §12.6.1 매핑.
- `harness-scaffold/SKILL.md` — 입력 스키마, §5.18 생성 규칙, 생성 순서, Phase 3 검증, Phase 4 보고, 카탈로그, §10.3 마이그레이션 주석, U1 재감지.
- `references/harness-checklist.md` — §4.2 @critical pre-push 게이트 = L4 강제 경로.
- `references/versioning-policy.md` — 1.14.0.
- `references/project-context.md` — 설계 결정·버전 히스토리.
- `.tracking/{HANDOFF,CHANGELOG,TODO}.md` — 트래킹.

---

## Task 1: pre-push 훅 템플릿 + 게이팅 골든 픽스처 (TDD 코어)

**Files:**
- Create: `test/prepush-fixtures.sh`
- Create: `templates/githooks/pre-push`

- [ ] **Step 1: 실패하는 픽스처 러너 작성**

Create `test/prepush-fixtures.sh` (전체 내용):

```bash
#!/bin/bash
# pre-push 훅 골든 픽스처 — 스킬 자체 검증 (생성 프로젝트와 무관, footprint 0)
#
# 목적: templates/githooks/pre-push 의 게이팅 분기를 결정적으로 검증 (mock playwright)
#   validate fail-fast / @critical 매칭 실행+exit 전파 / 0-매칭 no-op / 미설치 no-op / 마커 존재
#   + harness-check.sh ⑨ pre-push 게이트 활성/비활성 보고 (경고 전용)
#
# 요구: git, sh, node. 사용법: bash test/prepush-fixtures.sh
set -u
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
HOOK_TMPL="$ROOT/templates/githooks/pre-push"
HC="$ROOT/templates/harness-check.sh"
TMP="$(mktemp -d)"
trap 'rm -rf "$TMP"' EXIT
FAILS=0
N=0

# 워크 디렉토리 = git repo + 렌더된 훅 + mock playwright
# $1=validate_cmd  $2=mock_critical(0/1)  $3=mock_e2e_exit
make_work() {
  N=$((N + 1)); W="$TMP/w$N"
  mkdir -p "$W/node_modules/.bin"
  ( cd "$W" && git init -q )
  sed -e "s#{{VALIDATE_COMMAND}}#$1#g" "$HOOK_TMPL" > "$W/pre-push"
  chmod +x "$W/pre-push"
  cat > "$W/node_modules/.bin/playwright" <<MOCK
#!/bin/sh
case "\$*" in
  *--list*) [ "$2" = "1" ] && echo "  [chromium] > e2e/specs/login.e2e.ts:3:1 > critical"; exit 0 ;;
  *) exit $3 ;;
esac
MOCK
  chmod +x "$W/node_modules/.bin/playwright"
  echo "$W"
}
run_hook() { ( cd "$1" && sh ./pre-push </dev/null >/dev/null 2>&1 ); echo $?; }

echo "═══ pre-push 훅 픽스처 ═══"

# 1. 렌더 후 미치환 플레이스홀더 없음
W=$(make_work true 0 0)
if grep -q '{{.*}}' "$W/pre-push"; then echo "❌ 미치환 플레이스홀더 잔존"; FAILS=$((FAILS+1)); else echo "✅ 플레이스홀더 치환됨"; fi

# 2. validate 실패 → exit 1 (push 차단), E2E 도달 전
W=$(make_work false 1 0); RC=$(run_hook "$W")
[ "$RC" = "1" ] && echo "✅ validate 실패 시 차단(exit 1)" || { echo "❌ validate 실패인데 exit $RC"; FAILS=$((FAILS+1)); }

# 3. @critical 매칭 + E2E 통과 → exit 0
W=$(make_work true 1 0); RC=$(run_hook "$W")
[ "$RC" = "0" ] && echo "✅ @critical 통과 시 exit 0" || { echo "❌ @critical 통과인데 exit $RC"; FAILS=$((FAILS+1)); }

# 4. @critical 매칭 + E2E 실패 → exit 1 (push 차단)
W=$(make_work true 1 1); RC=$(run_hook "$W")
[ "$RC" = "1" ] && echo "✅ @critical 실패 시 차단(exit 1)" || { echo "❌ @critical 실패인데 exit $RC"; FAILS=$((FAILS+1)); }

# 5. @critical 0-매칭 → no-op exit 0 (e2e_exit=1이어도 실행 안 됨)
W=$(make_work true 0 1); RC=$(run_hook "$W")
[ "$RC" = "0" ] && echo "✅ @critical 0개 시 no-op(exit 0)" || { echo "❌ 0-매칭인데 exit $RC"; FAILS=$((FAILS+1)); }

# 6. playwright 미설치 → no-op exit 0
W=$(make_work true 1 1); rm -f "$W/node_modules/.bin/playwright"; RC=$(run_hook "$W")
[ "$RC" = "0" ] && echo "✅ playwright 미설치 시 no-op(exit 0)" || { echo "❌ 미설치인데 exit $RC"; FAILS=$((FAILS+1)); }

# 7. 마커 블록 존재 (멱등 주입 앵커)
grep -q "harness-setup:e2e-prepush:start" "$HOOK_TMPL" && grep -q "harness-setup:e2e-prepush:end" "$HOOK_TMPL" \
  && echo "✅ 마커 블록 존재" || { echo "❌ 마커 누락"; FAILS=$((FAILS+1)); }

# ── harness-check ⑨ (Task 2에서 구현 — 그 전엔 8~10 실패가 정상) ──
render_hc() { sed -e "s#{{LINT_ARCH_COMMAND}}#true#g" -e "s#{{VALIDATE_COMMAND}}#true#g" \
  -e "s#{{DOC_CHECK_COMMAND}}#true#g" -e 's#{{PATH_ALIAS_LIST}}#"@/"#g' "$HC"; }

# 8. .githooks/pre-push 있고 core.hooksPath 미설정 → ⑨ "비활성" 경고
N=$((N+1)); W="$TMP/w$N"; mkdir -p "$W/.githooks"; ( cd "$W" && git init -q )
render_hc > "$W/hc.sh"; cp "$HOOK_TMPL" "$W/.githooks/pre-push"
( cd "$W" && bash hc.sh 2>&1 | grep -q "비활성" ) && echo "✅ ⑨ 비활성 보고" || { echo "❌ ⑨ 비활성 미보고"; FAILS=$((FAILS+1)); }

# 9. core.hooksPath=.githooks → ⑨ "활성"
N=$((N+1)); W="$TMP/w$N"; mkdir -p "$W/.githooks"; ( cd "$W" && git init -q && git config core.hooksPath .githooks )
render_hc > "$W/hc.sh"; cp "$HOOK_TMPL" "$W/.githooks/pre-push"
( cd "$W" && bash hc.sh 2>&1 | grep -q "pre-push 게이트 활성" ) && echo "✅ ⑨ 활성 보고" || { echo "❌ ⑨ 활성 미보고"; FAILS=$((FAILS+1)); }

# 10. 훅도 hooksPath도 없음 → ⑨ 섹션 자체 생략
N=$((N+1)); W="$TMP/w$N"; ( cd "$W" 2>/dev/null || { mkdir -p "$W"; cd "$W"; }; git init -q )
render_hc > "$W/hc.sh"
( cd "$W" && bash hc.sh 2>&1 | grep -q "── ⑨" ) && { echo "❌ pre-push 없는데 ⑨ 실행됨"; FAILS=$((FAILS+1)); } || echo "✅ pre-push 미설치 시 ⑨ 스킵"

echo ""
echo "═══ 판정 ═══"
[ "$FAILS" -eq 0 ] && { echo "✅ 전체 통과"; exit 0; } || { echo "❌ ${FAILS}건 실패"; exit 1; }
```

- [ ] **Step 2: 픽스처 실패 확인 (템플릿 부재)**

Run: `bash test/prepush-fixtures.sh; echo "exit=$?"`
Expected: FAIL — `sed: .../templates/githooks/pre-push: No such file or directory` 및 `exit=1` (템플릿이 아직 없음). 케이스 8~10도 실패(⑨ 미구현) — Task 2에서 해소.

- [ ] **Step 3: 훅 템플릿 작성**

Create `templates/githooks/pre-push` (전체 내용):

```sh
#!/bin/sh
# harness-setup:e2e-prepush:start — harness가 관리하는 블록. 직접 수정 시 업그레이드에서 보존되지 않을 수 있다.
# pre-push 게이트: validate → @critical E2E (이슈 #12 증분 2b).
# ⚠️ 보안: 이 훅은 push 시 임의 코드를 실행한다. 신뢰할 수 있는 저장소에서만 활성화하라.
# monorepo: repo-root 기준 동작. 하위 패키지의 playwright는 미탐지될 수 있다(명시적 한계).
ROOT=$(git rev-parse --show-toplevel) || exit 1
cd "$ROOT" || exit 1

# ① validate (빠른 정적+유닛, fail-fast) — 스캐폴드 시점 패키지매니저로 치환됨
{{VALIDATE_COMMAND}} || exit 1

# ② @critical E2E — 실제 설치된 playwright + 매칭 스펙이 있을 때만 (PM 비종속)
PW="$ROOT/node_modules/.bin/playwright"
if [ -x "$PW" ]; then
  if "$PW" test --list --grep @critical 2>/dev/null | grep -q '\.e2e\.ts'; then
    "$PW" test --grep @critical || exit 1
  else
    echo "ℹ️ @critical E2E 스펙 없음 — pre-push E2E 게이트 건너뜀"
  fi
else
  echo "ℹ️ playwright 미설치 — pre-push E2E 게이트 건너뜀 (의존성 설치 후 활성)"
fi
# harness-setup:e2e-prepush:end
```

- [ ] **Step 4: 게이팅 케이스(1~7) 통과 확인**

Run: `bash test/prepush-fixtures.sh; echo "exit=$?"`
Expected: 케이스 1~7 모두 ✅. 케이스 8~10은 아직 ❌(⑨ 미구현) → 전체 `exit=1`. 1~7이 ✅이면 훅 게이팅 로직 정상.

- [ ] **Step 5: 커밋**

```bash
git add templates/githooks/pre-push test/prepush-fixtures.sh
git commit -m "feat(templates,test): pre-push 훅 템플릿 + 게이팅 골든 픽스처 (이슈 #12 증분 2b)"
```

---

## Task 2: harness-check ⑨ — pre-push 게이트 검사 (경고 전용)

**Files:**
- Modify: `templates/harness-check.sh` (헤더 주석 9행 + ⑧ 블록 뒤 ⑨ 추가)

- [ ] **Step 1: 헤더 주석에 ⑨ 반영**

`templates/harness-check.sh:9` 의 `#   경고 전용 (⑥⑦⑧) — exit code에 영향 없음` 를 다음으로 교체:

```
#   경고 전용 (⑥⑦⑧⑨) — exit code에 영향 없음
```

- [ ] **Step 2: ⑨ 검사 블록 추가**

`templates/harness-check.sh` 의 ⑧ 블록 끝(`fi` — `# 종합 판정` 직전, 현재 138행 `fi` 다음)에 다음을 삽입한다. `# 종합 판정` 주석 바로 앞:

```sh
# ⑨ pre-push 게이트 활성 여부 (경고 전용 — .githooks/pre-push 또는 core.hooksPath 설정 시에만)
HOOKS_PATH=$(git config --get core.hooksPath 2>/dev/null)
if [ -f .githooks/pre-push ] || [ -n "${HOOKS_PATH:-}" ]; then
  echo ""
  echo "── ⑨ pre-push 게이트 ──"
  ACTIVE_HOOK="${HOOKS_PATH:+$HOOKS_PATH/pre-push}"
  if [ -n "$ACTIVE_HOOK" ] && [ -f "$ACTIVE_HOOK" ] && grep -q "harness-setup:e2e-prepush" "$ACTIVE_HOOK" 2>/dev/null; then
    echo "✅ pre-push 게이트 활성 — core.hooksPath=$HOOKS_PATH"
  elif [ -f .githooks/pre-push ] && grep -q "harness-setup:e2e-prepush" .githooks/pre-push 2>/dev/null; then
    echo "⚠️ pre-push 훅(.githooks/pre-push)은 있으나 비활성 — 활성화: git config core.hooksPath .githooks"
  else
    echo "⚠️ pre-push 마커 미발견 — 게이트 미설치 (e2e.prePush 옵트인 필요)"
  fi
  if [ -x node_modules/.bin/playwright ]; then
    echo "✅ playwright 실행 가능"
  else
    echo "ℹ️ playwright 미설치 — E2E 게이트는 no-op (validate만 실행)"
  fi
fi
```

> ⑨는 `STRUCT_FAIL`/`QUALITY_FAIL`을 건드리지 않는다 — 판정과 분리(난제 ⑨). 경고 전용.

- [ ] **Step 3: 픽스처 전체 통과 확인**

Run: `bash test/prepush-fixtures.sh; echo "exit=$?"`
Expected: 케이스 1~10 모두 ✅, `exit=0` (케이스 8 비활성·9 활성·10 스킵).

- [ ] **Step 4: 기존 e2e 픽스처 회귀 없음 확인**

Run: `bash test/e2e-fixtures.sh; echo "exit=$?"`
Expected: ✅ 전체 통과, `exit=0` (⑧ 검사 영향 없음 — ⑨는 ⑧ 뒤 독립 블록).

- [ ] **Step 5: 커밋**

```bash
git add templates/harness-check.sh test/prepush-fixtures.sh
git commit -m "feat(templates): harness-check ⑨ pre-push 게이트 검사 (경고 전용, 판정 분리)"
```

---

## Task 3: 프로필 스키마 `e2e.prePush` + 옵트인 질문

**Files:**
- Modify: `SKILL.md` (e2e 스키마 ~610, 옵트인 질문 ~356)
- Modify: `harness-scaffold/SKILL.md` (e2e 스키마 ~144)

- [ ] **Step 1: SKILL.md 프로필 출력 스키마에 prePush 추가**

`SKILL.md` 의 e2e 스키마 블록을 교체:

```jsonc
  "e2e": {
    "enabled": true,
    "framework": "playwright",
    "playwrightVersion": "1.48.0",
    "prePush": true
  },
```

- [ ] **Step 2: SKILL.md 옵트인 질문 추가**

`SKILL.md` E2E 옵트인 블록의 두 번째 불릿(`- 동의하면 프로필에 e2e 필드(enabled, framework: "playwright", playwrightVersion)를 기록한다...`) **뒤**에 다음 두 줄을 추가:

```markdown
- **E2E 옵트인이 확정되고** `git rev-parse --is-inside-work-tree`가 참이면(git 저장소), 이어서 묻는다: "push 전에 `@critical` E2E 스펙을 자동 검증하는 pre-push 게이트를 추가할까요? `validate` 통과 후 `@critical` 태그 스펙만 실행합니다. **`.githooks/pre-push` 파일만 생성하며, 활성화 명령(`git config core.hooksPath`)은 셋업 후 직접 실행하셔야 합니다 — 스킬은 git 설정을 변경하지 않습니다.** git hook은 push 시 임의 코드를 실행하므로 신뢰할 수 있는 저장소에서만 권장합니다."
- 동의하면 `e2e.prePush: true`를 기록한다. E2E 미옵트인이거나 git 저장소가 아니거나 거부/무응답이면 `prePush`를 생략한다 (pre-push 산출물 미생성).
```

- [ ] **Step 3: harness-scaffold 입력 스키마에 prePush 추가**

`harness-scaffold/SKILL.md` 의 e2e 스키마 블록(`SKILL.md`와 동일 위치)을 Step 1과 **동일하게** 교체 (계약 동일성):

```jsonc
  "e2e": {
    "enabled": true,
    "framework": "playwright",
    "playwrightVersion": "1.48.0",
    "prePush": true
  },
```

- [ ] **Step 4: 스키마 동일성 + prePush 필드 규칙 추가**

`harness-scaffold/SKILL.md:181` 의 e2e 필드 설명 끝에 다음 문장을 추가한다 (기존 `... 새 플레이스홀더 아님).` 뒤):

```markdown
 `prePush`는 선택 하위 필드(생략=false)로 pre-push 게이트 생성 여부를 결정한다 (§ 5.18). `e2e.enabled` 없이 `prePush`만 있을 수 없다.
```

- [ ] **Step 5: 검증 — 스키마 동일성**

Run:
```bash
diff <(sed -n '/"e2e": {/,/}/p' SKILL.md) <(sed -n '/"e2e": {/,/}/p' harness-scaffold/SKILL.md) && echo "IDENTICAL"
```
Expected: `IDENTICAL` (양쪽 e2e 블록 동일).

- [ ] **Step 6: 커밋**

```bash
git add SKILL.md harness-scaffold/SKILL.md
git commit -m "feat(skill): e2e.prePush 프로필 필드 + 옵트인 질문 (git repo 게이트, 수동 활성화 고지)"
```

---

## Task 4: harness-scaffold §5.18 — pre-push 생성 규칙 + 공존성 분기

**Files:**
- Modify: `harness-scaffold/SKILL.md` (§5.17 뒤에 §5.18 신설)

- [ ] **Step 1: §5.18 신설**

`harness-scaffold/SKILL.md` §5.17 E2E 스캐폴드 모듈의 마지막 불릿(`- E2E 그린은 앱별 부팅...구조만 보장한다...`) **뒤**, `## 6. Phase 3: 검증` 앞에 다음 섹션을 삽입:

````markdown
### 5.18 pre-push 게이트 (옵트인 — e2e.prePush)

프로필에 `e2e.prePush: true`일 때만 실행한다. 없으면 이 단계 전체를 건너뛴다 (산출물 0건). `e2e.enabled`가 전제다.

> **원칙 준수**: 스캐폴드는 **`git config`를 실행하지 않는다.** `.githooks/pre-push` 파일만 생성/주입하고, 활성화는 Phase 4 안내로 사용자가 직접 수행한다 ("승인 없이 git 실행 금지" 절대 규칙).

#### 생성 파일

| 파일 | 카테고리 | 템플릿 | 치환 |
|------|---------|--------|------|
| `.githooks/pre-push` | managed | `templates/githooks/pre-push` | `{{VALIDATE_COMMAND}}` ← 프로필 scripts.validate |

#### 공존성 분기 (기존 git hook 환경 감지 — 텍스트 파싱만, 비실행 원칙)

생성 전에 기존 환경을 감지하여 분기한다 (`git config --get core.hooksPath`, `.husky/pre-push`·`.git/hooks/pre-push` 존재 여부 — 파일 실행/평가 금지):

| 환경 | 동작 |
|------|------|
| **그린필드** (hooksPath 미설정 + `.husky/pre-push` 없음 + `.git/hooks/pre-push` 없음) | `.githooks/pre-push`를 템플릿으로 신규 생성(`chmod +x`). Phase 4가 `git config core.hooksPath .githooks` 안내 |
| **기존 hooksPath/Husky** (`core.hooksPath` 설정됨 또는 `.husky/pre-push` 존재) | 그 경로의 `pre-push`에 **마커 블록(`harness-setup:e2e-prepush:start`~`end`)만 주입**(파일 끝에 append, 없으면 shebang+블록으로 생성). git config 변경 불필요 — 이미 활성 |
| **`.git/hooks/pre-push` 존재** (기본 경로에 사용자 훅) | core.hooksPath로 전환하면 기본 훅이 무력화되므로 **자동 전환·생성하지 않는다** → 폴백 |
| **비표준/파싱 불가/판단 불가** | **폴백**: 수정하지 않고 Phase 4에 권고 스니펫(마커 블록 + 활성화 명령)을 출력한다. 비활성 보고. 에러 아님 |

- **멱등**: 마커 `harness-setup:e2e-prepush:start`가 대상 파일에 이미 있으면 스킵.
- 주입 시 **shebang은 복제하지 않는다** — 마커 블록(start~end)만 기존 훅 뒤에 붙인다(호스트 훅의 shebang/로직 보존). 신규 생성 시에만 템플릿 전체(shebang 포함)를 쓴다.
- 훅 본문은 `set -e`가 아니라 명시적 `|| exit 1`을 쓴다 — 주입 시 호스트 훅 동작을 바꾸지 않기 위해 (템플릿이 이미 그렇게 작성됨).
````

- [ ] **Step 2: 검증 — §5.18 + 템플릿 정합**

Run:
```bash
grep -q "### 5.18 pre-push 게이트" harness-scaffold/SKILL.md && echo "✅ §5.18"
grep -q "harness-setup:e2e-prepush:start" templates/githooks/pre-push && echo "✅ 마커 일치"
```
Expected: `✅ §5.18` 와 `✅ 마커 일치` 둘 다 출력.

- [ ] **Step 3: 커밋**

```bash
git add harness-scaffold/SKILL.md
git commit -m "feat(skill): harness-scaffold §5.18 pre-push 생성 규칙 + 공존성 4-환경 분기"
```

---

## Task 5: harness-scaffold 배선 — 생성 순서·검증·Phase 4·카탈로그

**Files:**
- Modify: `harness-scaffold/SKILL.md` (생성 순서 ~221, §6.11 ~1083, Phase 3 검증 추가, Phase 4 보고, 카탈로그 ~1238, 렌더링 규칙 ~1251)

- [ ] **Step 1: 생성 순서에 pre-push 단계 추가**

`harness-scaffold/SKILL.md` 생성 순서 19번(`19. E2E 스캐폴드 모듈 ...`) **뒤**에 19-b를 삽입:

```markdown
19-b. pre-push 게이트 (e2e.prePush 옵트인 시에만 — § 5.18): .githooks/pre-push 생성/주입 (git config 미실행)
```

- [ ] **Step 2: §6.11 플레이스홀더 스캔에 훅 포함**

`harness-scaffold/SKILL.md:1083` 의 `grep -r '{{.*}}' agents/ .claude/rules/ init.sh scripts/doc-freshness.ts scripts/harness-check.sh docs/TECH_DEBT.md` 명령에 `.githooks/pre-push`를 추가한다 (파일이 있을 때만 검사되도록 존재 가드는 grep가 자동 처리):

```bash
grep -r '{{.*}}' agents/ .claude/rules/ init.sh scripts/doc-freshness.ts scripts/harness-check.sh docs/TECH_DEBT.md .githooks/pre-push 2>/dev/null && echo "❌ 미치환 플레이스홀더 발견" || echo "✅ 플레이스홀더 모두 치환됨"
```

- [ ] **Step 3: Phase 3 검증에 6.17 추가**

`harness-scaffold/SKILL.md` 의 `# 6.16 E2E 스캐폴드 검증` 블록 뒤에 다음을 삽입:

```bash
# 6.17 pre-push 게이트 검증 (e2e.prePush 옵트인 시에만)
if [ -f .githooks/pre-push ]; then
  grep -q "harness-setup:e2e-prepush" .githooks/pre-push && echo "✅ pre-push 마커 존재" || echo "❌ pre-push 마커 누락"
  grep -q '{{' .githooks/pre-push && echo "❌ pre-push 미치환 플레이스홀더" || echo "✅ pre-push 플레이스홀더 치환됨"
fi
```

- [ ] **Step 4: Phase 4 보고 — 활성화 안내 블록**

`harness-scaffold/SKILL.md` §7 보고에서 fenced 카탈로그 블록 **뒤, "#### 이제 할 수 있는 일 카탈로그 렌더링 규칙" 산문 앞**에 다음 산문을 추가:

```markdown
#### pre-push 게이트 활성화 안내 (e2e.prePush 옵트인 시에만)

`e2e.prePush`로 `.githooks/pre-push`를 생성/주입한 경우, 보고 말미에 다음을 **반드시** 출력한다 (스킬은 git 설정을 변경하지 않았다):

1. **활성화** (그린필드 생성 시): `git config core.hooksPath .githooks` — 기존 hooksPath/Husky에 주입한 경우 이미 활성이므로 이 명령을 안내하지 않고 "기존 훅에 주입됨(이미 활성)"으로 보고한다. 폴백한 경우 권고 스니펫과 수동 안내를 출력한다.
2. **보안 고지**: "이 훅은 `git push` 시 `validate` → `@critical` E2E를 실행합니다 (임의 코드 실행). 신뢰할 수 있는 저장소에서만 활성화하세요."
3. **monorepo 한계**: repo-root 기준으로 동작하므로 하위 패키지에 설치된 playwright는 미탐지될 수 있다 (명시적 한계).
4. **비활성화**: `git config --unset core.hooksPath`.
```

- [ ] **Step 5: 카탈로그에 pre-push 줄 추가**

`harness-scaffold/SKILL.md:1238` 의 브라우저 E2E 줄(`- 브라우저 E2E 회귀 작성 → ...`) **뒤**에 추가:

```markdown
- pre-push 게이트 → `git push` 시 `@critical` 자동 검증 (상세: .githooks/pre-push · references/harness-checklist.md §4.2) — e2e.prePush 옵트인 + 훅 생성 시에만 표시
```

- [ ] **Step 6: 카탈로그 렌더링 규칙에 pre-push 불릿 추가**

`harness-scaffold/SKILL.md` "#### 이제 할 수 있는 일 카탈로그 렌더링 규칙"의 **브라우저 E2E 줄 불릿 뒤**에 추가:

```markdown
- **pre-push 게이트 줄**: `e2e.prePush === true`이고 § 5.18 산출물(`.githooks/pre-push`)이 생성/주입된 경우에만 렌더 — 산출물 생성 신호를 재사용하는 순수 투영. 미옵트인·폴백 시 줄 자체를 생략한다 (미와이어/미활성 능력 광고 불가).
```

- [ ] **Step 7: 검증**

Run:
```bash
for s in "19-b. pre-push" "6.17 pre-push" "pre-push 게이트 활성화 안내" "pre-push 게이트 줄"; do
  grep -q "$s" harness-scaffold/SKILL.md && echo "✅ $s" || echo "❌ $s 누락"
done
```
Expected: 4개 모두 `✅`.

- [ ] **Step 8: 커밋**

```bash
git add harness-scaffold/SKILL.md
git commit -m "feat(skill): pre-push 배선 — 생성순서·Phase3 검증·Phase4 활성화 고지·카탈로그"
```

---

## Task 6: §12.6.1 매핑 + §10.3 마이그레이션 주석 + U1 재감지

**Files:**
- Modify: `SKILL.md` (§12.6.1 매핑 테이블 ~1137)
- Modify: `harness-scaffold/SKILL.md` (§10.3 주석, U1 재감지)

- [ ] **Step 1: §12.6.1 매핑에 pre-push 추가**

`SKILL.md:1137` 의 `| scripts/harness-check.sh | templates/harness-check.sh |` 행 **뒤**에 추가:

```markdown
| `.githooks/pre-push` | `templates/githooks/pre-push` |
```

> **노트(플래그)**: 기존 e2e managed 파일(`playwright.config.ts`, `e2e/tsconfig.json`)은 이 매핑에 **부재**한다 (1.11.0 옵트인 모듈 선례 — 자동 감지 제외). pre-push는 보안·정확성 업데이트 전파가 필요해 매핑에 포함한다. 두 e2e 파일의 매핑 정렬은 별도 후속(증분 4 / 본 증분 범위 밖)으로 TODO에 남긴다.

- [ ] **Step 2: §10.3 "마이그레이션 불필요" 주석에 1.14.0 반영**

`harness-scaffold/SKILL.md` §10.3 의 `#### M-1.4.0 이후 (1.5.0 ~ 현재): 마이그레이션 불필요` 섹션 본문에, 1.14.0이 마이그레이션 불필요인 근거 1줄을 추가한다 (해당 섹션 끝):

```markdown
> **1.14.0**(이슈 #12 증분 2b, pre-push 게이트)도 마이그레이션이 없다 — `.githooks/pre-push`는 신규 managed 파일이지만 `e2e.prePush` 옵트인 시에만 생성되고 생략=off라(1.11.0 e2e 모듈과 동일 선례), 기존 하네스는 산출물 0건이다. harness-check.sh ⑨ 추가분은 § 12.6 자동 감지로 전파된다. 신규 파일 생성은 U1 재감지 옵트인 경로로만 발생한다 (§ 10.2).
```

- [ ] **Step 3: U1 재감지에 prePush 옵트인 제안 추가**

`harness-scaffold/SKILL.md` § 10.2 (업그레이드 Phase U3~U5) 또는 U1 재감지 서술에서, 기존 `e2e.enabled` 하네스 업그레이드 시 prePush를 제안하는 1줄을 추가한다. e2e 재감지 서술 인접 위치에:

```markdown
- **pre-push 재감지** (1.14.0+): 기존 `e2e.enabled` 하네스에 `e2e.prePush`가 없고 git 저장소면, pre-push 게이트 옵트인을 **제안**한다 (생략 기본 — 거절/무응답 시 산출물 0건). 수락 시 § 5.18 생성 로직(공존성 분기 포함) 실행 + manifest 등록. 활성화는 Phase U5 보고에서 수동 안내(스킬은 git config 미실행).
```

- [ ] **Step 4: 검증**

Run:
```bash
grep -q '`.githooks/pre-push` | `templates/githooks/pre-push`' SKILL.md && echo "✅ 매핑"
grep -q "1.14.0.*증분 2b" harness-scaffold/SKILL.md && echo "✅ §10.3 주석"
grep -q "pre-push 재감지" harness-scaffold/SKILL.md && echo "✅ U1 재감지"
```
Expected: 3개 모두 `✅`.

- [ ] **Step 5: 커밋**

```bash
git add SKILL.md harness-scaffold/SKILL.md
git commit -m "feat(skill): pre-push §12.6.1 매핑 + 1.14.0 마이그레이션 불필요 + U1 재감지"
```

---

## Task 7: references — checklist §4.2 + versioning-policy 1.14.0

**Files:**
- Modify: `references/harness-checklist.md` (§4.2)
- Modify: `references/versioning-policy.md` (버전 카운트)

- [ ] **Step 1: harness-checklist §4.2 — pre-push 게이트 명시**

`references/harness-checklist.md` §4.2 (L4 E2E / 검증 레벨) 에서 1.12.0이 VERIFY 배선을 명시한 문장 인접에 다음을 추가 (정확한 앵커는 "VERIFY" 또는 "L4 E2E" 문장):

```markdown
- (1.14.0, 증분 2b) `@critical` 태그 E2E의 **cross-feature 회귀**는 옵트인 pre-push 게이트(`.githooks/pre-push`)가 push 시점에 강제한다 (`validate` → `@critical`). per-feature VERIFY(Phase 4.7)와 분리된 마지막 방어선이며, 활성화는 수동(`git config core.hooksPath`)이다. CI 부재 환경에서 "안 보이는 테스트" 방치를 막는 강제 경로.
```

- [ ] **Step 2: versioning-policy 1.14.0 기록**

`references/versioning-policy.md` 에서 최신 버전 카운트/히스토리 위치에 1.14.0 항목을 추가 (기존 1.13.x 항목 형식과 동일하게):

```markdown
- **1.14.0** (MINOR): 이슈 #12 증분 2b — pre-push 게이트. 신규 managed 파일 `templates/githooks/pre-push`(옵트인 `e2e.prePush`). 신규 플레이스홀더 0개(`{{VALIDATE_COMMAND}}` 재사용 — 30 불변). 마이그레이션 불필요(옵트인·생략 기본, 1.11.0 선례).
```

- [ ] **Step 3: 검증**

Run:
```bash
grep -q "증분 2b" references/harness-checklist.md && echo "✅ checklist §4.2"
grep -q "1.14.0" references/versioning-policy.md && echo "✅ versioning 1.14.0"
```
Expected: 둘 다 `✅`.

- [ ] **Step 4: 커밋**

```bash
git add references/harness-checklist.md references/versioning-policy.md
git commit -m "docs(refs): pre-push 게이트 — checklist §4.2 강제 경로 + versioning 1.14.0"
```

---

## Task 8: 버전 범프 + 트래킹

**Files:**
- Modify: `references/project-context.md`, `.tracking/CHANGELOG.md`, `.tracking/TODO.md`, `.tracking/HANDOFF.md`

- [ ] **Step 1: 버전 문자열 위치 확인**

Run:
```bash
grep -rn "1\.13\.1" SKILL.md harness-scaffold/SKILL.md references/project-context.md | head
```
Expected: 현재 버전 문자열 위치 목록. **주의**: 프로필/매니페스트 스키마의 `version` 필드가 있으면 1.14.0으로 갱신. (1.13.x → 1.14.0)

- [ ] **Step 2: 발견된 version 필드를 1.14.0으로 갱신**

Step 1에서 찾은 각 `version` 관련 항목(스키마 예시의 `harness.version`/프로필 version, project-context 현재 버전 표기)을 `1.14.0`으로 교체한다. 스키마 예시 JSON의 `"version": "1.13.1"` 류가 있으면 모두 교체.

- [ ] **Step 3: project-context.md 설계 결정 + 버전 히스토리**

`references/project-context.md` 의 § 설계 결정 사항에 추가:

```markdown
- **이슈 #12 증분 2b (1.14.0)**: pre-push 게이트. 활성화 수동(D1 — git config 비실행, "승인 없이 git 실행 금지" 정합), 게이팅 `validate`→`@critical`(D2), 옵트인 `e2e.prePush`(D3), eslint override 드롭(D4 — e2e/는 srcRoot 밖이라 assist 규칙 미도달, 승격 조건만 보존), 신규 플레이스홀더 0(D5). 공존성 4-환경 분기(그린필드/기존 hooksPath·Husky/기본 hooks/폴백) + 적응형 마커 주입. harness-check ⑨ 경고 전용(판정 분리). 설계 정본: docs/superpowers/specs/2026-06-16-e2e-prepush-2b-design.md
```

버전 히스토리에 `1.14.0` 줄 추가.

- [ ] **Step 4: CHANGELOG.md**

`.tracking/CHANGELOG.md` 에 1.14.0 엔트리 (Added/Changed):

```markdown
## [1.14.0] — 2026-06-16

### 추가 (Added)
- E2E pre-push 게이트 (이슈 #12 증분 2b): 옵트인 `e2e.prePush` → `.githooks/pre-push`(managed) 생성. `validate` → `@critical` E2E fail-fast 게이팅. 무의존 git hook(POSIX sh), PM 비종속(`node_modules/.bin/playwright`), `--list --grep @critical` 실제 매처.
- harness-check ⑨ pre-push 게이트 활성/비활성 보고 (경고 전용 — 판정 분리).
- 골든 픽스처 `test/prepush-fixtures.sh` (게이팅 분기 + ⑨ 회귀, mock playwright).

### 변경 (Changed)
- 활성화는 수동(`git config core.hooksPath` — 스킬 비실행, Phase 4 안내). 공존성 4-환경 분기 + 적응형 마커 주입.
- eslint e2e override는 드롭(YAGNI — e2e/는 srcRoot 밖). §12.6.1 매핑에 pre-push 추가.
- MINOR. 신규 플레이스홀더 0개(30 불변). 마이그레이션 불필요(옵트인·생략 기본).
```

- [ ] **Step 5: TODO.md — TODO-95b 완료 처리**

`.tracking/TODO.md` 의 TODO-95b 상태를 `[ ] 미착수` → `[x] 완료 (2026-06-16, 1.14.0)`로 변경하고, 해소 요약 1줄 추가:

```markdown
- **상태**: [x] 완료 (2026-06-16, 1.14.0)
- **해소**: 9개 난제 중 8개 정본 §8 방향대로 해소, 난제 ⑥(eslint override) 드롭(YAGNI — e2e/는 srcRoot 밖, 승격 조건 보존). 활성화 수동(D1). 골든 픽스처 통과. 설계 정본: docs/superpowers/specs/2026-06-16-e2e-prepush-2b-design.md. **후속 플래그**: e2e managed 파일(playwright.config.ts/e2e tsconfig)의 §12.6.1 매핑 정렬은 증분 4로 이월
```

- [ ] **Step 6: HANDOFF.md 갱신**

`.tracking/HANDOFF.md` 의 현재 버전 표기(`현재 버전: 1.13.1`)를 `1.14.0`으로, Session 추가 항목 1줄(Session 39 — 1.14.0 pre-push), P7 커버리지 테이블에 "1.14.0: pre-push 게이트(@critical cross-feature 강제, 옵트인)" 추가, § 5.2 우선순위에서 증분 2b를 완료로 이동하고 다음 후속(증분 3 MCP / 증분 4 프리셋·문서)을 즉시 다음으로 표기.

- [ ] **Step 7: 커밋**

```bash
git add references/project-context.md .tracking/
git commit -m "docs(tracking): 1.14.0 pre-push 게이트 — 버전 범프 + TODO-95b 완료 + 트래킹"
```

---

## Task 9: 최종 검증 — 골든 픽스처 전체 + 정합성

**Files:** (검증 전용 — 코드 변경 없음)

- [ ] **Step 1: 전체 골든 픽스처 실행**

Run:
```bash
bash test/run-fixtures.sh && bash test/e2e-fixtures.sh && bash test/prepush-fixtures.sh && echo "ALL FIXTURES PASS"
```
Expected: 3개 러너 모두 ✅, `ALL FIXTURES PASS`.

- [ ] **Step 2: 프로필 스키마 동일성 (CLAUDE.md 테스트 §정합성 ①)**

Run:
```bash
diff <(sed -n '/"e2e": {/,/}/p' SKILL.md) <(sed -n '/"e2e": {/,/}/p' harness-scaffold/SKILL.md) && echo "SCHEMA IDENTICAL"
```
Expected: `SCHEMA IDENTICAL`.

- [ ] **Step 3: 플레이스홀더 카운트 불변 (30)**

Run:
```bash
grep -oE '\{\{[A-Z_]+\}\}' harness-scaffold/SKILL.md | sort -u | wc -l
```
Expected: `30` (신규 플레이스홀더 0개 확인 — `{{VALIDATE_COMMAND}}`는 기존).

- [ ] **Step 4: 생성 파일 목록 ↔ templates/ 정합 (CLAUDE.md 테스트 §정합성 ③)**

Run:
```bash
test -f templates/githooks/pre-push && grep -q "templates/githooks/pre-push" harness-scaffold/SKILL.md && grep -q "templates/githooks/pre-push" SKILL.md && echo "✅ 템플릿 ↔ 스킬 참조 정합"
```
Expected: `✅ 템플릿 ↔ 스킬 참조 정합`.

- [ ] **Step 5: 미치환 플레이스홀더 정합성 — 훅 템플릿이 §6.11/6.17/픽스처에서 일관 참조**

Run:
```bash
grep -q ".githooks/pre-push" harness-scaffold/SKILL.md && echo "✅ §6.11/6.17 훅 참조"
```
Expected: `✅`.

- [ ] **Step 6: 스펙 커버리지 자가 점검 (코드 변경 없음 — 보고만)**

설계 정본 §12 난제 매핑 10개 각각이 어느 Task에서 구현됐는지 대조하여 보고한다:
- ① 마이그레이션 → Task 6 (1.11.0 선례, 별도 M-엔트리 없음)
- ② 공존성 → Task 4 (§5.18 4-환경 분기)
- ③ PM 비종속 → Task 1 (node_modules/.bin/playwright)
- ④ @critical 탐지 → Task 1 (--list --grep, 픽스처 케이스 3·5)
- ⑤ 실제 설치 판정 → Task 1 (-x, 픽스처 케이스 6)
- ⑥ eslint override → 드롭 (Task 없음 — D4, TODO 승격 조건 보존)
- ⑦ 멱등성/외부상태 → Task 2 (⑨ 활성/비활성, 픽스처 8·9)
- ⑧ 보안 고지 → Task 5 (Phase 4 활성화 안내 2)
- ⑨ 판정 분리 → Task 2 (⑨ 경고 전용)
- monorepo → Task 1 (훅 주석) + Task 5 (Phase 4 안내 3)

누락이 있으면 해당 Task로 돌아가 보완한다.

- [ ] **Step 7: (선택) 파일럿 도그푸딩 안내**

> harness-check(정적)는 런타임 push 게이팅을 검증 못 한다 (2a F3 교훈). 실제 강제는 haja 등 파일럿에서 `e2e.prePush` 옵트인 업그레이드 → `git config core.hooksPath .githooks` → 실패하는 @critical 스펙으로 push 차단 확인. 본 플랜 범위 밖(별도 도그푸딩 세션) — HANDOFF에 관찰 포인트로 기록.

---

## Self-Review 결과 (작성자 점검)

1. **Spec coverage**: 설계 §3(스키마)→T3, §4(훅/활성화)→T1·T4·T5, §5(난제)→T1, §6(⑨)→T2, §7(eslint 드롭)→T6 TODO, §8(마이그레이션/manifest/U1)→T6, §9(정합성)→T9, §10(파일)→전체, §11(테스트)→T1·T2·T9, §12(난제매핑)→T9 Step6. 갭 없음.
2. **Placeholder scan**: TBD/TODO 없음. 모든 코드 스텝에 완전한 내용. 버전 문자열만 Task 8 Step 1에서 grep로 동적 확인(현재 값이 환경 의존이라 명시적으로 grep 지시 — 플레이스홀더 아님).
3. **Type consistency**: 마커 `harness-setup:e2e-prepush:start/end` 전 Task 일관. `e2e.prePush` 필드명 일관. `{{VALIDATE_COMMAND}}` 기존 플레이스홀더 재사용 일관. `.githooks/pre-push` 경로 일관.

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
